framework:
    messenger:
        default_bus: command.bus

        buses:
            command.bus:
                middleware:
                    - validation
            query.bus: ~
            event.bus: ~

        transports:
            async: '%env(MESSENGER_TRANSPORT_DSN)%'
            sync: 'sync://'

        routing:
            App\Shared\Domain\Bus\Query: sync
            App\Shared\Domain\Bus\Command: async
            App\Shared\Domain\Event\DomainEvent: async
